spring:
  application:
    name: game-service

server:
  port: 8764

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
       defaultZone: http://127.0.0.1:8760/eureka/
  instance:
    hostname: localhost